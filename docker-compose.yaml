services:

  backend:
    build:
      context: ./backend

    container_name: backend

    ports:
      - "8000:8000"

    restart: always

  frontend:
    build:
      context: ./frontend

    container_name: frontend

    ports:
      - "3000:3000"

    restart: always

    depends_on:
      - backend